from django import forms

class PrioritiesForm(forms.Form):
    #subject = forms.CharField(max_length=100)
    #what firleds to use?
    #save preference items when button is clicked
    #preference1 = form.

class PreferencesForm(forms.Form):
    #message = forms.CharField(widget=forms.Textarea)
    city_size = forms.Integerfield
    sun = forms.
    temp = forms.